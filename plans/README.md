# Implementation Plans

Plans 005–009 were generated by a full `/improve` audit on 2026-07-09 at
commit `1d0df3d` (v11.2, corridor live). The maintainer was away; per the
skill's non-interactive default, plans were written for the top findings by
leverage — the full findings table is in the audit report delivered with
this run. Plans 001–004 are from the 2026-07-07 direction audit (commit
`9fda5d4`, pre-corridor-pivot) and were reconciled below rather than
re-audited.

Repo ground rules that bind every executor: deploys and pushes are
**user-gated** (`AGENTS.md`); the decision log is append-only; design laws
(no disclaimer copy, 1–2 word labels) apply to anything user-visible.
Executors: read your plan fully before starting, honor its STOP conditions,
and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 005  | Bound the live playback path (admission + backpressure) | P2 (was P1 — demoted by the 2026-07-09 "one viewer" decision; keep for robustness, not scale) | M | — | TODO |
| 006  | Harden the public HTTP surface | P1 | S–M | 005 first (same file) | TODO |
| 007  | Verification baseline: pytest + ruff + checks CI | P1 | M–L | — (coordinate line nos. with 005/006) | TODO |
| 008  | Docs & build-config truth pass (README v11, key untracked) | P2 | S | — | TODO |
| 009  | Frontend hot-path hygiene | P2 | S–M | — | TODO |
| 001  | Fleet-economics category in the shift report | P1 | S–M | — | SUPERSEDED — written against the v9 Berlin report layout; the fleet-economics wish itself survives (decision log 2026-07-05) and needs a fresh v11-scoped plan |
| 002  | Spatial outcome layer at shift end | P1 | M | — | SUPERSEDED — v9 full-Berlin scope; a corridor equivalent would be a new design |
| 003  | SPIKE: libsumo live custom-run mode on the Space | P2 | L | — | DONE (superseded by events) — the v11 corridor pivot shipped live libsumo per visit (commits `030276b`, `6f16258`) |
| 004  | Vision-onion doc refresh to v9 reality | P2 | S | — | SUPERSEDED — v9 is no longer the reality to document |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED / SUPERSEDED (with one-line rationale)

## Dependency notes

- 005 and 006 both edit `hf-space/app/main.py` — execute serially (005
  first). 007 creates tests that import that file; whichever lands last
  re-runs the full gate.
- 008 and 009 are independent of everything and of each other.
- 007 is the prerequisite for the two large refactors deliberately NOT
  planned this round (god-file split, TS strict burn-down).
- 008 Step 4 requires maintainer dashboard actions (GitHub repo variables,
  MapTiler key rotation) — the executor only reports the checklist.

## Maintainer decisions needed (not plannable by an executor)

- **Custom dispatch engine fate** (`engine=custom`, ~1,400 dormant lines in
  `main.py` with real bugs: hardcoded fleet 10 at `main.py:2463`, corridor
  demand regardless of scenario at `main.py:2511-2513`, tmp-file race at
  `main.py:1499-1510`): keep-and-fix as an engineering mode, or delete? A
  plan follows the call.
- **MapTiler key rotation + domain restriction** — see plan 008 Step 4.

## Findings recorded but not planned this round

- **Dead backend frame-builder chain** (`build_sumo_frame` /
  `live_traffic_light_states` / `display_traffic_light_state`,
  `main.py:5869-5941`, zero call sites): safe quick delete; fold into any
  future `main.py` plan.
- **TypeScript `strict` off** (`tsconfig.app.json` — no strict flags on a
  5k-line App.tsx): real, M-effort burn-down; sequence after 007 and 009.
- **God-file split** (`main.py` 5,962 / `App.tsx` 4,992 lines): highest
  long-term leverage, but gated on 007's characterization tests.
- **Per-rAF FeatureCollection rebuild** (`App.tsx:~874, ~3154`): profile at
  90x before optimizing — MED confidence it matters.
- **`/sumo/{scope}/network` payload size for `berlin`**: partially addressed
  by 006 Step 4 (cache headers); gzip/static precompute only if the Berlin
  scenario returns to the foreground.
- **`sortedWaits` memo keyed on `waits.length`**
  (`CybercabExperience.tsx:105`): latent trap, currently benign.
- **Single 1.28 MB JS chunk** (three.js + maplibre together, 352 KB gz):
  acceptable for a demo page; code-split only if load metrics say otherwise.
- **`scripts/` inventory** (24 scripts, several one-shot/legacy): a
  `scripts/README.md` grouping by scenario would do; not worth a full plan.

## Direction options (grounded in the decision log; for the maintainer)

- **Summon mode** ("enter an address, a cab comes") — the 2026-07-07 gate
  ("strictly dependent on live mode") is now OPEN: v11 runs live SUMO per
  visit. Still L effort (rider injection API + dispatch + UI); would be a
  design/spike plan first.
- **Fleet-economics report row** — wanted 2026-07-05 ("simple stats card…
  at the end"), never delivered; plan 001's v9 version is superseded; a
  v11 rescope is S–M (cost/ride, energy cost per evening on the 5-cab run).
- **Stochastic demand per visit** — parking-lot wish 2026-07-06 ("not always
  the same maybe"); live visits randomize the SUMO seed but demand is pinned
  to seed11's 21 riders. Committed seed files (seed1/11/12/13, 2, 3) and
  `scripts/build_demand_seeds.py` make sampling a demand seed per visit
  cheap — BUT the measured "100% served" ship numbers were calibrated on
  seed11; other seeds may serve less. Needs a calibration pass, not just a
  code change.

## Findings considered and rejected (kept from 2026-07-07 + this run)

- **Standalone time-window selector**: subsumed by live mode (shipped);
  revisit only as part of summon/custom-run UX.
- **German translation pass**: parked by the maintainer, not planned.
- **3D Cybercab monitor panel**: aspiration only; stays in the parking lot.
- **Auth/rate-limit middleware on the demo API**: public-anonymous is the
  product posture; 005/006 bound the abusable paths instead.
- **Client-side route interpolation**: explicitly against the runtime
  contract (SUMO is the source of truth) — never propose it.
