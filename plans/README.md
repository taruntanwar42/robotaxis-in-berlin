# Implementation Plans

Generated by the `/improve next` (direction) audit on 2026-07-07, at commit
`9fda5d4`. The maintainer was away; per the skill's non-interactive default,
plans were written for the top findings by leverage. Execute in the order
below unless dependencies say otherwise. Each executor: read the plan fully
before starting, honor its STOP conditions, and update your row when done.

Repo ground rules that bind every executor: deploys and pushes are
**user-gated** (`AGENTS.md`); the decision log is append-only; design laws
(no disclaimer copy, 1–2 word labels, "all input is error") apply to anything
user-visible.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Fleet-economics category in the shift report | P1 | S–M | — | TODO |
| 002  | Spatial outcome layer at shift end | P1 | M | — | TODO |
| 003  | SPIKE: libsumo live custom-run mode on the Space | P2 | L | — | TODO |
| 004  | Vision-onion doc refresh to v9 reality | P2 | S | 001–003 helpful, not required | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 and 002 are independent of each other and of 003; both are
  frontend-only and safe to execute in parallel sessions.
- 004 references 001–003 by number; executing it last keeps the roadmap doc
  accurate about their status, but it does not technically depend on them.
- 003 is a spike: its deliverable is measurements + a design note + a
  concurrency lock, NOT a deployed feature. The follow-up build plan should be
  written only after the spike's note exists.

## Findings considered and rejected

- **Standalone time-window selector** (logged 2026-07-06 as a later feature):
  subsumed by plan 003's live mode; a per-window recording matrix multiplies
  LFS weight (~19 MB × windows × fleets) for little story. Revisit only if the
  live-mode spike concludes negative.
- **German translation pass**: logged as optional-at-end (~30 min); parked by
  the maintainer, not planned.
- **3D Cybercab monitor panel**: logged 2026-07-05 as aspiration; no grounding
  beyond the wish; stays in the onion's parking lot.
- **Summon mode ("enter an address, go places")**: grounded (maintainer,
  2026-07-07) but strictly dependent on plan 003's outcome — deliberately not
  planned yet; plan 003 §Step 4 item 5 scopes what it would need.
